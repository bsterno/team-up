<div id="featured">

<div class="row">
  <div class="small-10 columns small-centered">
    <div class="create-row small-12 small-centered columns">
      <h1 id="index-header">Games</h1>
      <%=link_to "Create New Game", new_game_path, class:"create tiny button expand" %>
    </div>

    <table id="table_id" class="display">
      <thead>
        <tr>
          <th>Sport</th>
          <th>Address</th>
          <th>Date</th>
          <th>Time</th>
          <th>Additional info</th>
          <th>Players</th>
        </tr>
      </thead>

      <tbody>
        <% @games.each do |game| %>
        <tr>
          <td><%= game.sport.name %></td>
          <td><%= link_to game.street_address, game_path(game) %></td>
          <td><%= game.start_date %></td>
          <td><%= game.start_date.strftime("%l:%M%p") %></td>
            <% if game.description.empty? %>
              <td>N/A</td>
            <% else %>
              <td><%= game.description %></td>
            <% end %>
          </td>
            <% if game.players.count == game.max_players %>
          <td>FULL</td>
            <% else %>
          <td><%= game.players.count %>/<%= game.max_players %></td>
            <% end %>
        </tr>
        <% end %>
      </tbody>
    </table>

  </div>
</div>

</div>

<div class="panel">
  <div class="row">
    <hr>
    <div class="small-8 columns">© 2015 - Created by <%= link_to "Brian Stern", "https://github.com/bsterno" %></div>
    <div class="small-4 columns">
      <ul class="inline-list right">
        <li><a href="https://www.linkedin.com/in/sternbrian"><i class="fa fa-linkedin"></i></a></li>
        <li><a href="https://github.com/bsterno"><i class="fa fa-github"></i></a></li>
      </ul>
    </div>
  </div>
</div>

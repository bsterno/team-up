<h1>
  <%= @game.description %>
</h1>

<h3>
  <strong>Sport:</strong> <%= @game.sport.name %><br>
  <strong>Location:</strong> <%= @game.street_address %>, <%= @game.city %>
</h3>

<table>
  <thead>
    <tr>
      <th>Start Date/Time</th>
      <th>Player List</th>
      <th>Your Status</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @game.start_date %></td>
      <% if @players.count == @game.max_players %>
      <td>FULL</td>
      <% else %>
      <td><%= @players.count %>/<%= @game.max_players %></td>
      <% end %>

      <% if @player.nil? %>
        <td>Not Attending</td>
        <% if @players.count == @game.max_players %>
        <td>
          <% else %>
        <td><%= button_to "Join Game", game_players_path(@game), method: :post %></td>
          <% end %>
      <% else %>
        <td>Attending</td>
        <td><%= button_to "Leave Game", game_player_path(@game, @player), method: :delete %></td>
      <% end %>
    </tr>
  </tbody>
</table>

  <table>
    <thead>
      <tr>
        <th>Players</th>
      </tr>
    </thead>
    <% @players.each do |player| %>
    <tbody>
      <tr>
        <td><%= player.user.email %></td>
      </tr>
    </tbody>
    <% end %>
  </table>

<%= link_to "Back to Games", games_path %>

<% if @game.user == current_user %>
  <%= button_to "Delete Game", game_path, method: :delete, data: {confirm: "Are you sure you want to delete this game?"} %>
<% end %>
